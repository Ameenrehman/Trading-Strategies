.venv/
__pycache__/
*.pyc
.env
infra/.env
*.log

# Live trading state — this is a PUBLIC repo. positions.json holds real
# holdings and share counts, and orders_*.csv holds real order sizes.
# Publishing either would expose the actual portfolio and its size.
live/positions.json
live/orders_*.csv
live/paper_ledger.csv
live/paper_performance.csv
